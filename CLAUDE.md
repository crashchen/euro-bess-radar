# CLAUDE.md

This file provides working guidance for coding agents in this repository. Current model contracts take precedence over historical implementation notes.

## Current status and review ownership

Step 1 / 1b / 2 (#86–#88), Step 3A / 3B / 3C / 3D (#89–#92), Step 4 housekeeping (#93, `6d986e3`), the frontier input-identity/export-snapshot follow-up (#94, `bd4bb88`), and the frontier/floor export-provenance and failed-retry follow-up (#95, `cc6b1e2`) are merged. The multi-day/forecast-policy export-provenance correction is implemented on this review branch; see the [current verification snapshot](docs/validation/current.md). That snapshot binds commands, pass/skip/slow counts, CI and manual coverage to its recorded code head; do not treat an archived earlier run as verification of later behavior. [docs/audits/README.md](docs/audits/README.md) indexes revision-specific evidence.

The user invokes Claude Code / CC separately. Do not launch, resume, retry, schedule or delegate CC automatically. Prepare review material for the user. Historical references to Gemini/CC/Codex review rounds record past provenance, not standing authority to invoke an external reviewer. A reviewed draft is not authorization to merge: use the user's explicit merge instruction for that PR.

Import Steps 2–5 and older Phase/Increment labels below belong to their original implementation sequence; they are not the September audit's Step 1–4 status. Keep original handoffs and frozen patches unchanged when updating current summaries.

## Project

`euro-bess-radar` (a.k.a. eu-bess-pulse) is a local-first Streamlit dashboard for evaluating day-ahead arbitrage and merchant revenue potential of BESS across European bidding zones. Python 3.11+, pandas/numpy, SQLite cache, Plotly, openpyxl exports.

## Module Interaction (big picture)

`src/config.py` is the single source of truth for zones, timezones, cache paths, and revenue-math constants (`HOURS_PER_YEAR`, `ANCILLARY_CAPACITY_AVAILABILITY=0.95`, `ANCILLARY_ENERGY_ACTIVATION_SHARE=0.10`, `GBP_EUR_YEARLY`, `MAX_SHORT_GAP_HOURS=2.0`). Nothing else should hard-code zones, FX rates, or data-quality thresholds.

Data flow inside one dashboard run (`app.py`):
1. `data_ingestion.fetch_prices()` dispatches to the right backend (entsoe-py for ENTSO-E zones, Elexon REST for GB), runs `build_zone_query_window()` to convert local calendar dates to a UTC `[start, end)` window, and caches into `da_prices_{zone}` SQLite tables. Cache validity is checked per requested slice using row-level `fetched_at` plus a zone-aware expected grid, so stale rows and gaps are detected as missing points instead of accepted as sparse data. Known market-time-unit cutovers are segmented at their market-calendar boundary before reindexing.
2. `analytics.py` consumes the UTC price frame but groups by the zone's local calendar day via the `tz` parameter. `_find_daily_ordered_trade()` (called from `calculate_daily_spreads()`) finds the best chronology-aware charge→discharge pair with rolling windows sized by `duration_hours`. Heatmaps, P50/P90, RE correlation, and revenue all share this ordered-spread basis.
3. `ancillary_fetchers.py` exposes a lazy `getattr()` registry (`fetch_{zone}_ancillary`) used by `run_auto_fetch()`. Results flow into `ancillary.normalize_auto_fetch_dataset()` and then `build_ancillary_dataset()`, which merges with any user-uploaded manual CSVs — manual uploads override auto-fetch *only for the same product type*, preserving other auto-fetched products.
4. `merge_revenue_stack()` combines DA arbitrage with per-product ancillary revenue and returns a `source_revenues` dict (e.g. `DA Arbitrage`, `FCR-N`, `aFRR Up`) that both the Revenue tab and `export.py` read from. Capacity-reserve ancillary is not treated as fully additive with DA in the headline total; `gross_additive_total_eur` remains available as a non-co-optimized reference, and `dispatch.solve_joint_capacity_batch()` can provide a screening-grade joint MILP power-headroom estimate.
5. `export.py` writes Excel reports that must respect the same `tz` parameter and stacked-revenue breakdown as the dashboard.

Tests in `tests/` combine mocked/synthetic data, real local solvers and real Streamlit AppTest panels; they make no live API acceptance claim. They include cross-module contracts and do not map 1:1 to modules. `conftest.py` holds shared price/ancillary fixtures.

## Data Sources

### ENTSO-E Transparency Platform
- Requires API key (loaded from .env)
- Covers: EU-27 + Norway (NO_1–NO_5) + Switzerland (CH)
- Python library: entsoe-py (>=0.7.7, uses `web-api.tp.entsoe.eu`; endpoint overridable via `ENTSOE_ENDPOINT_URL` env var)
- Client construction must go through `data_ingestion._make_entsoe_client()` (`timeout=30`, `retry_count=1`). Repository decorators own auth-aware retry logging/backoff; restoring the entsoe-py default retry would multiply low-level requests.
- Returns: Day-ahead prices in EUR/MWh + generation by fuel type
- Resolution: SDAC zones use 60min before and 15min from SDAC market delivery day 2025-10-01; IE_SEM remains 30min, CH is outside that rollout, and GB is 30min via Elexon
- Historical data: available back to at least 2024 via the new API endpoint

### Elexon Insights API (Great Britain)
- NO API key required — fully public REST API
- Base URL: https://data.elexon.co.uk/bmrs/api/v1
- Endpoints:
  - GET /balancing/pricing/market-index — MID prices (GBP/MWh, 30-min)
  - GET /balancing/settlement/system-prices — system buy/sell prices (30-min)
  - GET /datasets/FUELINST — generation mix by fuel type
- GBP→EUR conversion uses year-specific rates via `GBP_EUR_YEARLY` dict in config.py
- Elexon returns multiple data providers per settlement period. Legitimate £0/MWh and negative prices must be retained; only recognizable duplicate zero placeholders are dropped before averaging remaining provider rows.

### Fingrid Open Data (Finland)
- API key recommended/required for v2 (`x-api-key` header via `FINGRID_API_KEY`)
- Base URL: https://data.fingrid.fi/api
- Dataset paths use `/datasets/{id}/data`
- Dataset IDs: 317 (FCR-N), 318 (FCR-D Up), 283 (FCR-D Down), 52 (aFRR Up), 51 (aFRR Down)
- Returns: hourly capacity prices in EUR/MW

### Regelleistung.net (Germany)
- NO API key required — public REST API
- Base URL: `https://www.regelleistung.net/apps/cpp-publisher/api/v1/download/tenders/resultsoverview`
- Params: `productTypes` (FCR/aFRR), `market` (CAPACITY), `exportFormat` (xlsx), `date` (YYYY-MM-DD)
- Auto-fetch enabled: downloads daily tender results as xlsx, parsed into standard ancillary format
- Manual CSV upload (`DE_FCR`, `DE_aFRR`) still supported as fallback and overrides auto-fetch for same product type
- Returns: capacity prices in EUR/MW

### Netztransparenz.de (Germany, reBAP/imbalance live fetch)
- Public German TSO transparency portal for balancing capacity/energy publications.
- Base URL in config: `NETZTRANSPARENZ_BASE_URL`.
- Implemented live fetch path: `data_ingestion.fetch_netztransparenz_imbalance()` calls the public `CsvDownloadHandler.ashx` chart endpoint for `NRV-Saldo qualitätsgesichert` (`ProduktId=6`) and `reBAP unterdeckt` (`ProduktId=10`, which carries both under/over symmetric reBAP columns), using the site's ME(S)Z/CET option so the returned `Zeitzone` column disambiguates DST.
- Writes through the same `persist_imbalance_frame(..., source="Netztransparenz.de")` cache/provenance path as manual reBAP/imbalance uploads; Data Trust and the cockpit overlay consume it unchanged.
- Not a replacement for Regelleistung.net tender-result capacity prices. GCC/LFC area balance remains manual/future.
- **Activated aFRR/mFRR energy (Steps 5a fetch + 5b persistence/UI)**: `data_ingestion.fetch_activation_energy("DE_LU", start, end)` joins TWO sources — quality-assured activated volumes from the same Netztransparenz chart handler (aFRR `ProduktId=1`, mFRR `ProduktId=17`; direction is a column split `Deutschland (Positiv/Negativ)` with non-negative magnitudes summing the four TSOs; ~1 month publication lag; FCR/k·Δf `ProduktId 7/8` deliberately excluded — no separately-paid energy leg) with ENTSO-E 17.1.f activation prices via `entsoe-py query_activated_balancing_energy_prices(DE_TENNET, process_type="A16")` (Germany-wide PICASSO/MARI marginal republished per control area — DE_TENNET/DE_50HZ/DE_TRANSNET identical, DE_AMPRION empty on the new API; price rows are SPARSE = only ISPs with actual activation). The ENTSO-E price call uses the same retry discipline as other ENTSO-E fetchers, with auth failures bypassing retry. Join on (15-min ISP, product, direction) after filtering volumes > 0. **Unpriced red-line (empirically calibrated)**: nonzero volume with no published price is dropped WITH explicit accounting (`attrs["unpriced_nonzero_intervals"]`/`attrs["unpriced_max_volume_mw"]` + warning) — never silently, and never treated as "no activation"; real months have such upstream gaps (2026-05-20 had up to 648 MW unpriced in all control areas), so a hard raise would make real windows unfetchable; an ALL-unpriced window still raises. Output = the 3b activation frame (`parse_activation_import_csv` parity, guarded by test). **Step 5b (LANDED — persistence + sidebar + Data Trust)**: the sidebar button **Fetch Netztransparenz + ENTSO-E activation energy** (Ancillary Services Data expander, DE_LU only, next to the activation CSV uploader) persists the fetched frame via `persist_activation_frame(source=ACTIVATION_SOURCE_NETZTRANSPARENZ_ENTSOE)` (`"Netztransparenz.de + ENTSO-E 17.1.f"` — ONE composite provenance stream, because each cached row is a volume×price join and cannot be re-attributed per source afterwards). The unpriced accounting graduates from transient attrs to durable state: `persist_activation_frame` reads the fetch frame's attrs and `activation_price_sources` gains nullable `unpriced_nonzero_intervals`/`unpriced_max_volume_mw` columns (ALTER-TABLE migration for pre-5b sidecars; reads fall back gracefully without migrating) with LAST-WRITE semantics — the fetch's window-global counts are stamped on every stream that write touches, untouched streams carry their previous values forward, and a manual-CSV write stores NULL ("no accounting available", distinct from an accounted 0). `imported_at` follows the same per-stream last-write semantics in BOTH the activation and capacity sidecars (a write to one stream no longer refreshes untouched streams' timestamps); the IDA and imbalance sidecars never had the issue — their writes are single-stream by construction. Data Trust's activation source table shows both columns ("Unpriced dropped" / "Unpriced max MW") and the sidebar success message repeats the dropped-unpriced count, so a lossy window is visible at fetch time and auditable later. No cockpit/overlay changes in 5b.

### ENTSO-E Imbalance Prices
- Uses same ENTSO-E API key as DA prices
- Covers: RO, SE_3, Italian bidding zones, and other ENTSO-E zones where imbalance data is available
- Returns: imbalance/balancing prices in EUR/MWh

### ENTSO-E Intraday Auction Prices (IDA1/2/3)
- Uses same ENTSO-E API key as DA prices via `entsoe-py.query_intraday_prices(zone, start, end, sequence)`.
- `INTRADAY_SUPPORTED_ZONES` (data_ingestion.py) lists the SIDC participants that publish auction results — currently `{DE_LU, NL, BE, FR, AT, IT_NORD}`. Other zones return `None` rather than raising.
- Sequence: 1 = IDA1 (15:00 D-1 opening auction), 2 = IDA2 (22:00 D-1), 3 = IDA3 (10:00 day-of-delivery). The dashboard currently uses IDA1 only.
- `entsoe-py` raises `NoMatchingDataError` (a `ValueError` subclass) when a supported zone has no IDA data for the requested window — this is mapped to `None`, not a hard error.
- Auth failures raise `DataSourceAuthError`; network failures raise `DataSourceNetworkError`.
- **Live IDA coverage is currently empty in practice**: ENTSO-E returns a `No matching data found` acknowledgement for the IDA auction query (`A44` + `contract_MarketAgreement.type=A07` + classificationSequence) across the tested SIDC zones/windows — an upstream data gap, NOT a code/parsing bug (verified at the raw-HTTP layer).
- **Manual IDA CSV fallback**: `parse_intraday_csv()` + `persist_intraday_frame()` (data_ingestion.py) let users upload IDA prices when the API has nothing. Minimal schema `timestamp, ida_price_eur_mwh` with optional per-row `sequence` and `zone`; rows write to the SAME `ida_prices_{zone}_seq{n}` SQLite tables the live fetch uses, so the Revenue/Cockpit cache-first reads consume them unchanged. `app.py` reads the IDA cache cache-first so the cockpit sees uploads without a Revenue-tab visit. `sequence` accepts `1/2/3` or `IDA1/IDA2/IDA3` labels (case-insensitive) and falls back to the UI default round only when blank — fractional (`1.5`) or non-numeric (`bogus`) values RAISE rather than silently coercing to the default round (which would file IDA2/IDA3 rows under the IDA1 table); validation runs only on rows surviving the timestamp/price dropna.
- **IDA provenance is durable AND per-row**: `write_intraday_cache(..., source=...)` stores the source on every price row (migrating pre-provenance tables by backfilling a legacy sentinel) and upserts a derived label into the `ida_price_sources` sidecar — uniform source ⇒ that label, multiple sources in one table ⇒ `Mixed`. Data Trust's `build_intraday_source_table()` reads that sidecar (via `read_intraday_sources()`), so a `(zone, sequence)` mixing manual + live intervals shows `Mixed` rather than only the last write's source, and labels survive a session restart.

## Geographic Scope

Three tiers of zones:

1. **EU member states** (ENTSO-E): DE_LU, FR, NL, BE, AT, PL, IT_NORD, IT_CNOR, IT_CSUD, IT_SUD, IT_CALA, IT_SICI, IT_SARD, ES, PT, RO, DK_1, DK_2, SE_1, SE_2, SE_3, SE_4, FI, CZ, HU, BG, GR, HR, SK, SI, EE, LT, LV, IE_SEM
2. **Non-EU ENTSO-E members** (same entsoe-py API): NO_1, NO_2, NO_3, NO_4, NO_5, CH
3. **Great Britain** (Elexon API): GB

## Ancillary Services

### Auto-Fetch (ancillary_fetchers.py)
Zone-specific fetchers run automatically when the zone is selected:
- **FI** — FCR-N/D + aFRR prices via Fingrid
- **DE_LU** — FCR/aFRR auction results via Regelleistung.net REST API (downloads daily xlsx)
- **GB** — System buy/sell prices via Elexon
- **RO, SE_3, IT_NORD, IT_CNOR, IT_CSUD, IT_SUD, IT_CALA, IT_SICI, IT_SARD** — Imbalance prices via ENTSO-E

Registry uses lazy `getattr()` lookup for testability. `run_auto_fetch()` handles per-fetcher errors gracefully.

### Manual Upload (CSV Templates)
Supported templates:
- **DE_FCR** — Germany FCR from regelleistung.net
- **DE_aFRR** — Germany aFRR from regelleistung.net
- **RO_BALANCING** — Romania balancing from Transelectrica
- **FI_FCR** — Finland FCR from Fingrid
- **GB_BALANCING** — GB system prices from Elexon/BMRS

Upload via sidebar. Template CSVs downloadable from the UI. A separate **Intraday (IDA) Prices** sidebar uploader handles IDA1/2/3 price CSVs (see the ENTSO-E Intraday section) — it does not use the `ANCILLARY_TEMPLATES` registry.

**Unified reserve-capacity import (Step 2, import-first)**: `ancillary.generate_capacity_import_template_csv()` is the single zone-tagged capacity format — columns `timestamp, zone, product, direction, capacity_price_eur_mw_h` (+ optional `timezone`), downloadable from the sidebar and documented in `docs/import-templates.md` (the spec to hand to an exchange/TSO). Two semantics are pinned in the template header: timestamps are UTC (or carry a `timezone` column), and `capacity_price_eur_mw_h` is a PER-HOUR rate, never a 4h-block total. The unified parser `ancillary.parse_capacity_import_csv()` (increment 6b) ingests this into the standard ancillary frame: `product`→`product_type` canonicalised to FCR/aFRR/mFRR and `direction`→up/down/symmetric (case/separator tolerant; an unrecognised value RAISES, like the IDA importer), per-row `timezone` honoured (else UTC), and `capacity_price_eur_mw_h` mapped to the existing `capacity_price_eur_mw` column (same unit, no codebase-wide rename). `product_type`/`direction` are kept faithful to the CSV (direction is NOT folded into the product label). Persistence (also 6b): `data_ingestion.persist_capacity_frame()` / `write_capacity_cache()` write one `capacity_prices_{zone}` table keyed `(timestamp, product, direction)` (keep-last on re-import via `INSERT OR REPLACE`), with a `capacity_price_sources` provenance sidecar keyed `(zone, product, direction)` — full parity with the IDA `ida_price_sources` path. The sidecar label is RE-DERIVED per stream after every write (uniform source ⇒ that label, multiple ⇒ `Mixed`, `read_capacity_sources()`), so an overwrite that changes the source refreshes provenance rather than leaving a stale label on fresh data; `imported_at` is per-stream LAST-WRITE time (streams the write touches get the new timestamp, untouched streams keep theirs). `read_capacity_cache()` reads it back as a standard frame. Data Trust (6c slice 1) renders a capacity-source table and shows reserve PER zone in the coverage matrix from this sidecar (see the Data Trust bullet). A sidebar uploader (6c slice 2, `sidebar._parse_and_store_capacity_upload`, in the **Ancillary Services Data** expander under **Unified Reserve Capacity CSV**) calls `parse_capacity_import_csv` + `persist_capacity_frame(source="Manual CSV")`, shows a per-(zone,product,direction) import summary, and surfaces `DataSourceParseError` as a friendly message — it coexists with (does NOT replace) the per-country session-ancillary uploader. Cockpit consumption (6c slice 3) closes the loop: the forecast-driven IDA-policy panel resolves capacity **cache-first** via `_resolve_capacity_dataset(primary_zone, anc_df)` — `read_capacity_cache(primary_zone)` (imported unified capacity) drives the 9.2b reserve rows, falling back to the session ancillary only when the cache is empty, with a "Reserve capacity source: Cached unified import / Session ancillary fallback" caption. The resolved full-zone frame is windowed per valid date via `_slice_to_local_dates` before pricing, so a full-sample mean never leaks into the comparison window. **Step 2 import-first loop is now closed end-to-end: template → upload → parse → SQLite + provenance → Data Trust → cockpit 9.2b model.**

**Activation-energy import (Step 3, import-first, started 3a):** the ENERGY leg of reserves — paid only when the TSO actually calls the reserve, distinct from the standing capacity fee. Following the same import-first playbook (template/spec → parser → Data Trust → cockpit replay; activation/GCC live fetchers only after their schema/semantics are stable, NOT first). **3a (template + spec only, no parser):** `ancillary.generate_activation_import_template_csv()` (constants `ACTIVATION_IMPORT_COLUMNS/PRODUCTS/DIRECTIONS`) — columns `timestamp, zone, product, direction, activation_price_eur_mwh, system_activated_volume_mw` (+ optional `timezone`); `product` is aFRR/mFRR (FCR has no separately-paid energy leg), `direction` is up/down (energy activation is directional, no symmetric). Downloadable from the sidebar (Ancillary Services Data expander) and specced in `docs/import-templates.md` §3. **Three red-lines pinned in the template header + docs (lock BEFORE the parser):** (1) `system_activated_volume_mw` is SYSTEM/area-level activated power, NOT this asset's output — the asset/capture share is a model assumption that lives in the audit panel, never pre-mixed into the CSV; (2) activation energy is a SEPARATE stream from the capacity fee AND from reBAP/imbalance settlement and is NOT free additive revenue (activating up spends SoC DA/IDA could have sold — it competes for the same energy budget); (3) with no forward activation signal it supports HISTORICAL REPLAY ONLY, never live dispatch or aggregator activation-following. **3b LANDED (parser + persistence + provenance, full parity with the capacity 6b path):** `ancillary.parse_activation_import_csv()` returns a DEDICATED activation frame (timestamp-indexed UTC; `zone, product_type, direction, activation_price_eur_mwh, system_activated_volume_mw`) — NOT the standard ancillary frame, because it carries energy-leg columns the standard frame lacks AND because activation energy is a separate stream. `product` canonicalised to aFRR/mFRR (**FCR RAISES** — no paid energy leg), `direction` to up/down (**`symmetric` and blank RAISE** — energy is directional, unlike capacity where blank⇒symmetric); unknown product/direction RAISE (IDA/capacity strictness); bad timestamp/price/volume rows DROP. `system_activated_volume_mw` is stored SYSTEM-level exactly as parsed — no asset/capture-share multiplication anywhere in the parser or persistence (the red-line). Persistence: `data_ingestion.persist_activation_frame()`/`write_activation_cache()` write one `activation_prices_{zone}` table keyed `(timestamp, product, direction)` (keep-last via `INSERT OR REPLACE`) + an `activation_price_sources` sidecar keyed `(zone, product, direction)` with the label re-derived per stream after each write (uniform⇒source, multiple⇒`Mixed`, else `Legacy`; `read_activation_sources()`), so an overwrite refreshes provenance. `read_activation_cache()` reads it back. Sidebar uploader (`sidebar._parse_and_store_activation_upload`, "Unified Activation-Energy CSV" under Ancillary Services Data) wired to the existing template download. The shared timestamp helper `ancillary._import_utc_index` (renamed from `_capacity_utc_index`) now serves both importers. **Zone-name safety (Gemini security review, PR #18):** raw CSV `zone` values flow into per-zone SQLite table names (`activation_prices_{zone}` / `capacity_prices_{zone}`), so `data_ingestion.validate_import_zone()` enforces table-name safety (`re.fullmatch(r"[A-Za-z0-9_]+")`, non-empty) at BOTH the parser layer (`ancillary._resolve_import_zone_cell`, early friendly error — also applied to the capacity parser) and the `_{activation,capacity}_cache_table()` choke point (guards a caller bypassing the parser). It is deliberately NOT the supported-bidding-zone check (`_validate_zone`) — import-first may ingest a zone not yet wired into config; this is table-name safety, not business support. **3c-1 LANDED (Data Trust only):** `data_trust.build_activation_source_table()` (reads `read_activation_sources()`) renders an "Activation-energy price sources" table at `(zone, product, direction)` granularity, and `build_coverage_matrix()` gains a PER-zone `activation_energy` column — import-only, so NO session fallback (unlike the reserve column). No model/revenue calc. **3c-2a LANDED (replay-calc only, no UI):** `activation_overlay.compute_activation_overlay(activation_df, *, reserve_mw, capture_share, interval_hours=None)` → `{activation_energy_overlay_eur, by_stream[(product,direction) → activated_mwh, activation_overlay_eur], reserve_mw, capture_share}`. Formula: `asset_activated_mwh = min(reserve_mw, capture_share · max(system_activated_volume_mw, 0)) · dt`; `cashflow = asset_activated_mwh · activation_price_eur_mwh`. **Locked semantics:** the activation price is NOT sign-flipped by direction (it is a cash-flow price already; a down activation at a negative price stays negative — no double-sign error); `system_activated_volume_mw` is floored non-negative (a negative is never a sign); `capture_share` is a global scalar (≥0, not auto-clamped above 1 — the `reserve_mw` cap bounds delivered power); `dt` inferred per stream from timestamp spacing (overridable). Direction is a breakdown label + future-SoC hook only. Pure calculation: NO UI, NO SoC consumption, NO MILP, NOT additive to the strategy total (the headline key is `activation_energy_overlay_eur`, deliberately not "revenue"). **3c-2b LANDED (cockpit panel):** `simulation_cockpit._render_activation_overlay_section` (gated on `read_activation_cache(primary_zone)` having data) — a capture-share slider (percent units, default 1%, range 0–10%; `app.py` converts to a fraction for the audit table), windows the activation cache to the loaded dates via `_slice_to_local_dates`, calls `compute_activation_overlay`, and shows the `activation_energy_overlay_eur` metric + per-(product,direction) breakdown under a hard caption (replay overlay; NOT co-optimized, NOT additive to the strategy total, no SoC coupling, not aggregator dispatch; assumes a regular interval series). `build_assumptions_table(activation_capture_share=…)` appends three audit rows (capture share / regular-interval interpretation / non-additive overlay basis). `interval_hours` is deliberately NOT exposed in the UI (it would imply the panel can fix dirty data); the regular-interval requirement is documented in `docs/import-templates.md` §3 instead. **Activation-energy import→model loop is now CLOSED end-to-end: template → upload → parse → SQLite + provenance → Data Trust → cockpit overlay.** **Steps 5a+5b (LANDED)** add the live path for this stream: `data_ingestion.fetch_activation_energy()` joins Netztransparenz activated aFRR/mFRR volumes with ENTSO-E 17.1.f prices into the same 3b activation frame, and the sidebar button **Fetch Netztransparenz + ENTSO-E activation energy** persists it through the SAME 3b cache/provenance path (composite source `Netztransparenz.de + ENTSO-E 17.1.f`), carrying the fetch's dropped-unpriced accounting into the `activation_price_sources` sidecar and Data Trust (see the Netztransparenz data-source section for endpoint/red-line/last-write-semantics details); GCC/LFC area balance remains future/manual.

**reBAP / imbalance settlement import (Step 4, import-first, 4a–4f):** the passive BRP/portfolio imbalance-settlement stream, distinct from reserve-capacity fees and activation-energy cashflows. **4a LANDED (template + spec):** `ancillary.generate_imbalance_import_template_csv()` (constant `IMBALANCE_IMPORT_COLUMNS`) — columns `timestamp, zone, imbalance_price_eur_mwh, system_imbalance_volume_mw` (+ optional `timezone`, documented in `docs/import-templates.md` §4) and downloadable from the sidebar (Ancillary Services Data expander). **Four red-lines pinned before model work:** (1) this is a separate passive settlement strategy, not capacity fee and not activation energy, so do NOT blindly add it to reserve revenue; (2) `system_imbalance_volume_mw` is SYSTEM/area imbalance, NOT this asset's imbalance — asset imbalance/share assumptions belong in the audit panel later; (3) `imbalance_price_eur_mwh` is the published cash-flow settlement price (negative values valid), with no extra direction sign flip; (4) historical replay/diagnostics only, not live BRP control or aggregator dispatch. **4b LANDED (parser + persistence + provenance + sidebar uploader):** `ancillary.parse_imbalance_import_csv()` returns a DEDICATED imbalance frame (timestamp-indexed UTC; `zone, imbalance_price_eur_mwh, system_imbalance_volume_mw`) — NOT the standard ancillary frame, because imbalance settlement is a separate passive stream. Bad timestamp/price/volume rows DROP; missing required columns or unsafe zones RAISE. `system_imbalance_volume_mw` and signed `imbalance_price_eur_mwh` are stored exactly as parsed — no asset-share multiplication and no direction sign flip anywhere in parser/persistence. Persistence: `data_ingestion.persist_imbalance_frame()`/`write_imbalance_cache()` write one `imbalance_prices_{zone}` table keyed by `timestamp` (keep-last via `INSERT OR REPLACE`) + an `imbalance_price_sources` sidecar keyed by `zone` with the label re-derived after each write (uniform⇒source, multiple⇒`Mixed`, else `Legacy`; `read_imbalance_sources()`), so an overwrite refreshes provenance. `read_imbalance_cache()` reads it back. Sidebar uploader (`sidebar._parse_and_store_imbalance_upload`, "Unified reBAP / Imbalance CSV" under Ancillary Services Data) is wired to the existing template download. Zone-name safety uses the shared `data_ingestion.validate_import_zone()` at parser and table-name choke-point. **4c LANDED (Data Trust only):** `data_trust.build_imbalance_source_table()` renders a "reBAP / imbalance settlement sources" table keyed by zone, and `build_coverage_matrix()` gains a PER-zone `imbalance_settlement` column showing `source (rows)` from `read_imbalance_sources()`; import-only, so NO session fallback. **4d-1 LANDED (replay-calc only, no UI):** `imbalance_overlay.compute_imbalance_overlay(imbalance_df, *, power_mw, capture_share, interval_hours=None)` → `{imbalance_settlement_overlay_eur, by_system_state[(system_short/system_long/neutral) → asset_imbalance_mwh, imbalance_overlay_eur], power_mw, capture_share, sign_convention}`. Sign convention was validated against German Netztransparenz May-2026 NRV-Saldo + reBAP CSVs and the official NRV-Saldo-Ampel wording: `system_imbalance_volume_mw > 0` = system short / undercovered, so positive BESS net dispatch (discharge / extra injection) helps; `< 0` = system long / overcovered, so negative BESS net dispatch (charge / less injection) helps. Formula: `asset_net_dispatch_mw = sign(system_imbalance_volume_mw) * min(power_mw, capture_share * abs(system_imbalance_volume_mw))`; `cashflow = asset_net_dispatch_mw * dt * imbalance_price_eur_mwh`. `imbalance_price_eur_mwh` remains the published signed cash-flow price — no second sign flip — so helping the system can still lose money when the price sign is unfavourable. Pure calculation basis: no SoC/energy sustainability, no MILP, and not additive to the strategy total (headline key is `imbalance_settlement_overlay_eur`, deliberately not "revenue"). **4d-2 LANDED (cockpit panel):** `simulation_cockpit._render_imbalance_overlay_section` (gated on `read_imbalance_cache(primary_zone)` having data) windows the imbalance cache to the loaded dates via `_slice_to_local_dates`, exposes a capture-share slider (percent units, default 1%, range 0-10%; `app.py` converts to a fraction for the audit table only when cached imbalance rows overlap the current zone/window, preventing Streamlit session-state leakage across zones), and shows the signed `imbalance_settlement_overlay_eur` metric + by-system-state breakdown. Hard caption: historical replay overlay only, NOT co-optimized, NOT additive to the strategy total, ignores SoC/energy sustainability (more optimistic than activation because passive imbalance can be sustained), and not live BRP control or aggregator dispatch. `build_assumptions_table(imbalance_capture_share=...)` appends four audit rows (capture share / sign convention / non-additive overlay basis / power cap). **Imbalance import->model loop is now CLOSED end-to-end: template -> upload -> parse -> SQLite + provenance -> Data Trust -> cockpit overlay.** **Manual Netztransparenz converter (4e):** `scripts/convert_netztransparenz_imbalance.py` converts official German `NRV-Saldo qualitaetsgesichert` + `reBAP unterdeckt/ueberdeckt` CSV exports into the unified imbalance template (`timestamp,zone,imbalance_price_eur_mwh,system_imbalance_volume_mw`). It is a thin CLI wrapper over `data_ingestion.convert_netztransparenz_imbalance_exports` (public API) — the SAME converter the 4f live fetch uses — so manual and live paths share one set of semantics: German comma-decimal normalisation, official `N.A.` unavailable intervals dropped (after the raw-axis 15-minute regularity check), NRV/reBAP inner join (only timestamps published in both survive; fully disjoint files raise), CEST/CET DST disambiguation via the `Zeitzone` column, symmetric under/over reBAP tolerance on published rows, and a final round-trip of the generated CSV through `parse_imbalance_import_csv`. See `docs/runbooks/validate-imbalance-overlay.md` for the May-2026 real-data smoke test. **Live Netztransparenz fetcher (4f):** `data_ingestion.fetch_netztransparenz_imbalance("DE_LU", start, end)` calls the public `CsvDownloadHandler.ashx` chart endpoint directly (base64-encoded `{LocalFrom, LocalTo, ResultTimeZone="cet", Settings}` payload), downloads `NRV-Saldo qualitätsgesichert` (`ProduktId=6`) + `reBAP unterdeckt` (`ProduktId=10`, includes both under/over columns), validates the raw response timestamp axes and German DST labels, explicitly parses German comma-decimal numbers, drops official unavailable value tokens such as `N.A.` before taking the NRV/reBAP inner join, validates reBAP under/over equality on published rows, returns the dedicated imbalance frame, and the sidebar button **Fetch Netztransparenz reBAP/imbalance** persists it with source `Netztransparenz.de`. Manual upload and live fetch therefore share one cache/provenance/Data Trust/cockpit path.

### Data Merging
`build_ancillary_dataset()` merges manual + auto-fetch data. Manual uploads override auto-fetch for the same product type only — other auto-fetched products are preserved. `normalize_auto_fetch_dataset()` converts varied fetcher schemas into a standard ancillary format with per-product rows. Override matching uses `_normalize_product_key()` (case- and separator-insensitive) so an upstream label flip from `aFRR Up` to `afrr_up` does not silently bypass the override.

### UI Guidance
- Revenue Estimation tab shows an annualisation note based on the currently selected sample window length
- Revenue Estimation tab includes a `How ancillary works` expander explaining product-level stacking, manual-vs-auto precedence, and which ancillary signals are not auto-monetised

### Auth Errors
Fingrid / Regelleistung / Elexon all detect HTTP 401/403 via the shared `_raise_if_auth_failed()` helper and raise `DataSourceAuthError` *before* the `@retry` decorator can swallow it. `run_auto_fetch()` deliberately does not catch `DataSourceAuthError`, so it propagates to `_run_and_store_ancillary_fetch()` in `src/components/sidebar.py` which surfaces it via `st.error` with a hint (e.g. "Set FINGRID_API_KEY in .env."). Other transient fetcher errors (network, parse) are still caught per-fetcher and logged.

### Data Quality
`clean_prices()` short-gap interpolates internal gaps up to `MAX_SHORT_GAP_HOURS` (config.py) and flags those rows with `imputed=True`. The threshold and `max_source_gap_hours` are measured from real timestamp spans, not missing-row counts at one dominant cadence; longer or edge gaps stay NaN with `filled=True / imputed=False`. `_segment_and_reindex_prices()` also detects sparse internal gaps when no `expected_window` is provided. For zones in `config.SDAC_15MIN_ZONES`, it resolves `SDAC_15MIN_DELIVERY_DATE` through the shared `SDAC_MARKET_TIMEZONE` (CET/CEST), then reindexes the pre- and post-transition segments at 60 and 15 minutes respectively. This surfaces gaps on either side without upsampling the old grid or dropping the new one; cache validation uses the same contract. Do not resolve this cutover through each country's civil timezone: FI, PT, PL, GR, and RO all switch at the same `2025-09-30T22:00:00Z` source timestamp. See `docs/design/price-resolution-transition-v1.md`. `analytics.filter_to_complete_local_days(df, tz)` mirrors the day-level filter that `calculate_daily_spreads()` uses, and is applied in `app.py` and `src/export.py` so negative-price stats and heatmaps stay consistent with daily-spread / dispatch / revenue analytics across the dashboard, Excel, and PDF outputs. Excel/PDF exports always recompute the negative-price stats from the filtered subset internally; do not rely on the caller-supplied `negative_stats` to be already filtered.

## Coding Conventions
- pandas timestamps are UTC internally; every analytics/export entry point takes a `tz` parameter for local-time grouping. Do not group by UTC day — daily spread and heatmap numbers will be wrong across DST boundaries.
- Add new zones in `config.py` only: `ENTSOE_ZONES`/`ELEXON_ZONES` + `ZONE_TIMEZONES`. Never hard-code zone codes elsewhere.
- SQLite table naming is `da_prices_{zone_code}` lowercase (e.g. `da_prices_de_lu`).
- GBP→EUR conversion must use `_get_gbp_eur_rate_for_year()` (nearest-year fallback against `GBP_EUR_YEARLY`), never a single scalar rate — GB history spans multiple FX regimes.
- API calls retry with exponential backoff, max 3 retries.
- Keep functions under ~50 lines; split long pipelines at natural stage boundaries.
- Logging via `logging`, Google-style docstrings, type hints on public functions.
- The repository supports Streamlit >=1.55,<2.0 and tests a 1.55.0 compatibility subset. Use `width="stretch"` for `st.dataframe` / `st.plotly_chart`; preserve the established container-aware metric layout.

## Key Domain Knowledge
- **Ordered spreads**: Revenue estimation uses chronology-aware charge-before-discharge windows, not simple max-min. `_find_daily_ordered_trade()` finds best non-overlapping buy/sell pair using rolling averages and backward scan.
- **Duration-aware analysis**: `duration_hours` parameter (1h/2h/4h) controls rolling window size for spread calculation, heatmaps, and zone comparison. Models actual BESS dispatch constraints.
- **Zone-local time**: All analytics group by local calendar day/hour (via `tz` parameter), not UTC. Critical for correct daily spread, heatmap, and export date ranges.
- SDAC zones switched from 60- to 15-min DA resolution at the shared CET/CEST market midnight for delivery day 2025-10-01 (`2025-09-30T22:00:00Z`), not at each country's civil midnight
- GB data is 30-min settlement periods, GBP/MWh (not EUR)
- **Sub-hourly resolution**: `calculate_negative_price_hours()` returns both `negative_hours` (physical hours) and `negative_intervals` (count of data points) to handle 15-min/30-min data correctly
- Norway and Switzerland are NOT EU members but their TSOs are ENTSO-E members — their data is on the Transparency Platform
- Some ENTSO-E zones have data gaps — always validate returned DataFrame is not empty
- DA prices across ENTSO-E are in EUR/MWh; GB is in GBP/MWh
- Generation data: not all zones have solar/wind/offshore split — handle missing columns gracefully
- Negative wind/solar-price correlation = BESS-friendly market (high RE → low prices = charging, low RE → high prices = discharging)
- **Revenue stacking**: `merge_revenue_stack()` exposes DA arbitrage + ancillary `source_revenues` for per-product breakdown, but capacity-reserve ancillary is not assumed to be fully additive with DA in the headline total without co-optimization.
- **Joint co-optimization**: `solve_joint_capacity_batch()` models reserve capacity as power headroom competing with DA charge/discharge in a MILP with binary charge/discharge mutual exclusion. It does not model activation energy, bid acceptance, reserve-specific SoC duration, or qualification constraints.
- **Mutual exclusion is binary, not relaxed**: `solve_daily_lp()` and `solve_daily_joint_capacity_lp()` both use a binary mode variable via scipy `integrality`. A pure LP relaxation of `p_charge + p_discharge <= power_mw` is degenerate at negative prices (the solver can earn revenue by simultaneously charging and discharging through round-trip losses). Do not weaken this back to an LP-only constraint.
- **Dispatch failure contract** (`docs/design/dispatch-failure-contract-v1.md` + `docs/design/dispatch-failure-contract-v2.md`): ordinary/joint, composite DA+ID/reserve, sequential, replay, and stochastic solver results return typed `success/status/message` state. Every batch separates missing from solver-failed days, preserves stage-qualified diagnostics, and excludes failures from revenue, annualisation, stochastic risk pools, and common-policy windows. Continuous replay rejects incomplete local days; a failed chunk contributes no rows and cannot advance SoC. Preserve audit metadata through analytics/UI/export paths; an all-failure result is unavailable, never a genuine €0 market result or a trigger for a hidden ceiling fallback.
- **Economic semantics** (`docs/design/economic-semantics-v1.md`): installed CapEx, the linear wear proxy, and cash NPV are three different concepts. Gross dispatch revenue is the market result; `calculate_degradation_cost()` is a non-cash shadow price for economic-margin/cycle-policy screening; project cash NPV must not deduct that proxy again. Only explicitly timed augmentation/replacement costs may enter cash NPV. The current double-leg denominator is labeled two-leg throughput cost, not conventional discharge-output LCOS.
- **ProjectCase PC-A/PC-B/PC-C/PC-D boundary** (`src/project_case/`, `src/pages/project_case.py`, locked contracts `docs/design/project-case-v1.md` and `docs/design/project-case-contract-settlement-v1.1.md`): PC-A emits typed/fingerprinted producer results and the immutable `RunResult` scaffold. StrategyRunResult remains fingerprint schema v1; ProjectCase/RunResult use v1.1. The pure `compute_project_case()` path bootstraps the one total-EUR daily series once, reuses each annual draw across the horizon, discounts year by year, and returns the no-lifecycle-cost screening NPV plus the pre-tax unlevered lifecycle cash NPV. Screening excludes fixed O&M; lifecycle adds only explicit fixed O&M, dated augmentation/replacement net of event salvage, and terminal residual/decommissioning. `UNKNOWN` remains screening-only with the exact typed unavailable lifecycle result. With no `ContractCase`, numerical/table-column arithmetic stays on the PC-B path; with the sole v1.1 annual whole-project strategy-cash-floor basis, settlement applies `max(M[d,t], F[t])` to every bootstrap draw/project year before lifecycle costs and uses a rank-interpolated P50 reconciliation path. The legacy wear-net contracted-floor overlays, shadow wear, VOM re-deduction, MW re-scaling, tax, debt, and financing never enter Project Case cash. PC-C owns the Revenue-tab strategy selector and explicit run/cache boundary, the Cockpit read-only mirror, strict augmentation/projection CSV imports, and RunResult-only Excel export. The selector exposes only data-supported producer lanes, defaults to DA-only, forces composite strategies to flat projection, visibly audits partial date coverage, and invalidates stale results on every relevant input/version change. **PC-D3 adds interactive `ContractCase` entry and disclosure** in that same panel: an optional "Contracted floor settlement" expander (default = no contract, so merchant-only cash is unchanged) resolves flat/escalating/explicit quote entry into the explicit real rate curve that is actually validated and fingerprinted, binds `target_base_year` to `ValuationCase.base_year` and modelled MW to `AssetCase.power_mw` (never a second knob), keeps `quote_basis`/`asset_scope`/`settlement_frequency` as displayed fixed literals, and enforces the locked source/status/document-hash null matrix (an uploaded source document is sent to the app backend to compute its SHA-256 and outranks a typed digest, with only the digest entering ContractCase, the cache, the fingerprint and the export — the panel says so explicitly and points users on an untrusted deployment at an external `shasum -a 256`, so do NOT restore a "hashed locally" or "never uploaded" claim without browser-side hashing; typed input is whitespace-stripped but never case-folded, so a non-lowercase digest fails loudly). The pre-run validation preview renders the resolved floor through `valuation.resolve_effective_contract_floor()` — the same single implementation of `F[t] = q_k · MW · a_k` that `compute_project_case()` settles against, so a previewed curve cannot drift from the settled one. Every contract term is in the PC-C request fingerprint, so any contract edit invalidates the cached result before it renders or downloads. Post-run disclosure is derived from the `RunResult` alone (never from the live widgets): the locked product-boundary sentence, the `max(M,F)`/uncapped-top-up statement, the entitlement-factor and real-EUR currency assertions, the resolved per-year floor table, and the rank-interpolated P50 + reconciliation disclosure; the lifecycle caption flips from "contracted-floor settlement excluded" to "contract settlement IS included" and the read-only Cockpit mirror says so too. A term that leaves project life, a wrong-length or non-numeric curve, and a provenance/assertion disagreement all fail closed rather than falling back to merchant-only. The v1 reserve profile is the German six-slice nominal-4h product only: capacity settlement is DST-normalised separately from physical energy/SoC `dt`; FI reserve is intentionally fail-closed until an hourly profile/forecast contract increment lands (FI DA remains supported).
- **PC-D product boundary**: `AnnualPreLifecycleStrategyCashFloor` is a user-asserted, real-base-year-EUR, whole-modelled-project annual strategy-cash floor—not accounting/legal gross revenue, MACSE, a complete legal contract, or a bankable valuation. Negative merchant cash is not clamped before `max(M,F)`, so top-up can exceed the quoted positive floor. Capped negative-year top-up, MACSE/term-sheet-specific settlement, partial contracted MW, fees/sharing/penalties, tax, debt, and monthly settlement remain deferred tagged products; never adapt the wear-net comparator into this aggregator.
- **Annualisation caveat**: DA arbitrage revenue is extrapolated from the user-selected sample window, so short windows (for example winter-only periods) can materially overstate or understate full-year merchant potential
- **Cycle-cap × degradation frontier** (`src/cycle_frontier.py` + cockpit panel, contract `docs/design/cycle-cap-frontier-v1.md`, F-A/F-B/F-C all landed): `compute_cycle_cap_frontier()` sweeps `solve_daily_lp(max_efc_per_day=cap, min_throughput_tiebreak=True)` per clean local day over a cap set (default `(0.5, 1.0, 1.2, 1.5, 2.0, 3.0, None)`, `None` = uncapped reference) and nets a LINEAR wear proxy EX-POST (`capex_eur_kwh · capacity_kwh / cycle_life` per FEC via `calculate_degradation_cost` — the optimiser stays wear-blind; no DoD/C-rate/temperature model). Red-lines: DA-only per-day standalone terminal-neutral basis (no IDA/reserve/carry interaction in v1); IDENTICAL valid-day set across caps (a day excluded for data or solver reasons — `success=False` on ANY cap — drops from EVERY row); FEC accounting uses raw discharged energy `sum(p_discharge[t] * dt[t]) / capacity` (never the rounded `n_cycles` field); a scalar duration is the uniform special case. Best-cap rule: LOWEST finite cap within tolerance (`NET_TOL_EUR_PER_MW_YR=1.0`, converted to window EUR) of the max net wins; uncapped only on a strict win. `frontier_flat` flags a within-tolerance frontier; `n_tiebreak_fallback_days` counts days where any cap's min-FEC pass fell back (#43 pattern). The uplift-vs-uncapped denominator is ABSOLUTE (sign stays meaningful at negative uncapped net; NaN when the uncapped net is within tolerance of zero). **Cockpit panel (F-C)**: `simulation_cockpit._render_cycle_frontier_section` — expander immediately AFTER the multi-day replay section (wiring order is AST-pinned); inputs are a cap multiselect (`accept_new_options` free entry; invalid entries warned + ignored via `_parse_frontier_caps`), a `cycle_life` number input, and a window selectbox, while capex is INHERITED from the sidebar (single source of truth, no second knob — the panel captions the inherited value). Output = 4 KPI metrics + ONE grouped gross-vs-net EUR/MW/yr bar chart (not two charts — the gap IS the wear) + a best-row-flagged/net-highlighted table + `frontier_flat` info and tie-break-fallback warning. Excel export via `cockpit_tables_to_excel` with `_append_frontier_assumptions` (7 provenance rows: wear model red-line, cost/FEC, EUR/MWh-discharged, cycle life, capex source, DA-only revenue basis, co-temporal annualisation) on top of `_cockpit_export_assumptions(capture_value="Not applied…")`; the global `build_assumptions_table` is deliberately NOT extended in v1. Results are cached in `st.session_state`. **Frontier follow-up, merged in #94:** `_frontier_fingerprint` continues to return a tuple but replaces the old first/last/count date identity with a hash of the full supplied DA frame and every selected local date. It includes the panel version, live `dispatch.DISPATCH_VOM_COST_EUR_MWH`, annualisation constants in `cycle_frontier` and `degradation`, the best-cap tolerance, and zone/caps/cycle life/timezone/asset/liquidity inputs. An edit outside the selected dates conservatively invalidates the result too. Unrelated reruns and downloads do not solve; changed consumed inputs withhold the result/download and return no floor context, while reverting them restores the stored frontier. The export assumptions are built only after a successful Run and saved in the result bundle. If the incoming global assumptions table is `None`, the saved export assumptions remain `None` and no panel-specific provenance rows are added. Theme and unrelated sidebar assumptions do not change the solver identity or overwrite that snapshot. Old in-session bundles require a fresh run through the version change. The dependent floor clears its own result while the frontier is stale, so restoring the frontier does not restore the floor result; the floor requires its own Run. See the frontier contract and dated handoff. The Styler hides its own index (`hide(axis="index")` — `st.dataframe` ignores `hide_index=True` for Styler input), and the expander title uses the contract-locked `×` (RUF001 noqa'd). An AppTest headless smoke drives the real panel in CI (`tests/test_simulation_cockpit_frontier.py`): chart trace names, table row count + single ★ matching the best-cap metric, download button, rerun persistence, fingerprint invalidation. **Realised dispatch VWAPs (reporting addendum, PR #55)**: every frontier row (table + Excel) also carries `charge_vwap_eur_mwh`/`discharge_vwap_eur_mwh` — energy-weighted physical DA prices of the actual solved schedule, accumulated window-globally as `Σ(price·MWh)/Σ(MWh)` over the common valid days (NEVER a mean of daily VWAPs — regression-pinned; the hot loop accumulates value/energy in NumPy and divides once per cap). The single-day helper is `analytics.calculate_dispatch_price_vwaps()` (raises on misaligned/non-finite input; a missing physical leg is `NaN`, never a fabricated zero price; negative charge prices stay signed; VOM and capture haircut excluded — it prices the schedule at the raw market price). The DA-only single-day cockpit shows the same VWAPs as a caption via `_build_result(include_market_vwap=True)` (`market_vwap_available` summary key); DA+IDA1 replay deliberately exposes NONE (`include_market_vwap=False`) — its two-market financial settlement has no single realised market price. Reporting layer only: no solver, revenue, wear, best-cap, or red-line change (see the reporting addendum in the contract doc).
- **Contracted-floor cash-flow comparison (CF-A + CF-B LANDED — contract `docs/design/contracted-floor-v1.md`)**: `contracted_floor.compute_contracted_floor_overlay()` is a pure, solver-independent comparison of the cycle-frontier's wear-net DA-only merchant baseline `M` with an availability-adjusted annual floor `F`; protected cash flow is `max(M, F)`, never `M + F`. It returns annual merchant/floor/top-up values and contract-window operating-cash-flow PVs using the shared public `scenario.annuity_pv_factor`; these are NOT project NPVs and contain no dispatch, financing, tax, credit, post-term, or multi-stream assumptions. All numeric inputs must be finite; merchant remains signed; with `M < 0` and `F = 0`, the protected result is zero and the top-up is `-M`. CF-B adds a cockpit expander immediately after the cycle frontier and is strictly gated on a current best-row result: quote/availability/tenor/discount-rate inputs are panel-local, percentages are divided by 100 at the UI boundary, and a result fingerprint includes both frontier provenance and the selected row's recomputed values so stale merchant baselines cannot survive a data correction. Outputs are annual merchant/effective-floor/protected/top-up cash flows plus contract-window PVs, one separate grouped chart, a formula-input table, and a self-contained `Contracted floor` Excel sheet with full source assumptions. The floor never enters the strategy comparison or changes dispatch; the hard screening/legal/credit/tax/financing/post-term caveat remains visible. Its numerical inputs and selected merchant baseline remain fingerprinted. Merged #95 makes frontier and floor Excel copies identify the actual DA-only MILP, no sidebar capture haircut and frontier-wear CapEx basis; the frontier panel version invalidates old export snapshots. A successful floor Run saves its panel-correct assumptions, and explicit failed frontier/floor retries clear their previous success; the underlying cash and dispatch calculations are unchanged.
- **Decaying merchant × contracted-floor composition (contract LOCKED via PR #65; FD-A + FD-B LANDED via PR #67 `baf5570` + PR #68 `385b609` — `docs/design/contracted-floor-decay-v2.md`)**: `contracted_floor.compute_decaying_contracted_floor_overlay()` projects the frontier's stored wear-net year-1 merchant cash flow as `M_t = N + G·(weight_t−1)` while holding wear flat, compares it per year with an escalating effective floor, and returns an auditable trajectory plus merchant/protected/top-up contract-window PVs. Inactive inputs delegate to the shipped v1 overlay bit-identically; active trajectories are capped at `MAX_FLOOR_TRAJECTORY_YEARS = 100` before materialisation. FD-B adds panel-local decay/minimum-share/floor-escalation inputs, keeps the Revenue-tab assumptions display-only, extends the stale-result fingerprint with gross and all three trajectory inputs, and exports the discounted per-year reconciliation. The floor remains protective (`max(M_t, F_t)`), never additive; negative late merchant years are reported, not clamped; no dispatch, solver, pandas, financing, tax, credit, or post-term value is introduced.
- **DA liquidity participation cap (LH-A + LH-B LANDED — contract `docs/design/liquidity-haircut-v1.md`)**: `liquidity.compute_liquidity_cap()` is a pure calculation of `executable_power_mw = min(installed power, max participation share x user-entered average single-sided zone DA volume in MWh/h)`. It is a feasible-volume cap, NOT a revenue multiplier, price-impact/depth model, compliance threshold, or fetched-data claim. `cycle_frontier.compute_cycle_cap_frontier(executable_power_mw=...)` passes the scalar only to `solve_daily_lp(power_cap_mw=...)`; installed power still determines energy capacity, raw EFC, linear wear, EUR/MW/yr annualisation, and best-cap tolerance. `None` preserves the historical path, every cycle-cap row including the cycle-uncapped reference shares the same liquidity cap, both charge/discharge directions are clipped, and the summary echoes the applied cap. LH-B adds an opt-in checkbox inside the frontier panel; the user must assert average single-sided zone DA volume, the share is entered in percent and converted at the UI boundary, and no volume proxy/default is used. Inputs enter the frontier fingerprint, export five panel-local provenance rows, and flow into the contracted-floor baseline caption/assumptions; a binding cap selects the locked liquidity-capped source label while the floor quote and annual cash-flow scaling remain per installed MW. DA-only scope; no IDA/reserve/stochastic, global assumptions, fetched-volume claim, price impact, or double application of the capture haircut.
- **Annual merchant-revenue decay (SD-A + SD-B LANDED — contract `docs/design/spread-decay-v1.md`)**: `scenario.decaying_annuity_pv_factor()` projects the Revenue tab's year-1 merchant cash flow with `weight_t = max((1-d)^(t-1), floor)`; year 1 stays at 100%, fractional life follows the existing annuity convention, and inactive inputs (`d=0` or `floor=1`) delegate to `annuity_pv_factor` so the legacy NPV expression remains bit-identical. `calculate_npv_distribution()` and `sensitivity_table()` apply the decayed factor to merchant revenue only while any explicitly supplied annual cash cost keeps the flat annuity factor. Under `economic-semantics-v1.md`, the Revenue page passes zero for that cash-cost argument: its throughput wear proxy is non-cash and reported separately. SD-B adds percent-entered decay/floor controls to Revenue Estimation's Risk Analysis, explicit terminal-year and model-class disclosures, and NPV-sorted tornado directions; the bootstrapped year-1 revenue metrics stay unchanged. This is a revenue-space screening assumption, not a price-series transform or re-dispatch model. The separate cockpit floor panel now supports its own decaying-merchant composition under `contracted-floor-decay-v2.md`; do not treat the historical flat-only floor stage as the current behavior.
- **Portfolio analysis** (`src/portfolio.py`): treats each zone as a daily-revenue-per-MW series and runs Pearson correlation, Sharpe-like mean/std, and a long-only Markowitz frontier via scipy SLSQP. `build_daily_revenue_matrix()` inner-joins on dates so a single missing local day in any zone drops that date from the whole portfolio view — this keeps correlations and frontier weights computed on a single common sample. Annualisation uses the same `365.25`-day convention as `estimate_annual_arbitrage_revenue` and assumes i.i.d. days (`std_annual = std_daily * sqrt(365.25)`); for short or seasonal samples treat the Sharpe number as a relative ranking, not an absolute investment metric.
- **Forward scenarios** (`src/forward_curve.py`): user-supplied forward-curve CSV (zone, delivery_start, delivery_end, price_eur_mwh, optional contract/shape/source/as_of). `build_forward_synthetic_prices` overlays each contract's baseload onto the historical hour-of-week shape for the same zone: `hourly_price[t] = forward_base * historical_shape[hour_of_week(t)]`. The synthetic series is then fed through the existing daily-spread / MILP-dispatch / annualisation pipeline, so the dashboard reuses one consistent revenue model across historical and forward views. Overlapping contracts (same zone, e.g. a Cal-2027 + Jan-2027 quote) keep the later one in CSV order on the overlap region; the UI also lists the overlap rows in a warning so users can resolve the intent themselves. Shape recovery requires the zone's historical DA series to be loaded in the sidebar first. **External trader benchmark v1 (LANDED — PR #71 `d9b0b18`)** adds `trader_benchmark.py` + a Forward Scenarios expander: a user-uploaded annual EUR/MW/yr curve is joined to the platform curve only on equal zone/calendar-year, with controlled `asset_type`/`market_scope`/`revenue_basis` plus optional duration/cycle/source/as-of metadata. The platform side is explicitly forward-synthetic DA-only revenue AFTER the sidebar capture haircut and BEFORE wear/fees/tax/financing. `benchmark_to_model_ratio` is a descriptive reconciliation statistic, NEVER an inferred capture rate; unlike asset/scope/basis/duration/cycle metadata produces explicit warnings, and non-overlap remains provenance-only. The benchmark can never become a price curve, floor, solver input, or automatic adjustment; Enspired co-location/cross-market slides are architecture references only and remain deferred until renewable forecast, shared-grid/PPA opportunity-cost, and Italian ancillary schemas are locked. See `docs/design/external-trader-benchmark-v1.md`.
- **Data Trust tab** (`src/data_trust.py`, `src/pages/data_trust.py`): displays one row per fetched zone with source label, timezone, first/last UTC timestamp, coverage %, source gaps, short-gap imputations, unresolved missing intervals, and max gap hours. Keep this as the user-facing audit label for screening outputs whenever data ingestion or cleaning semantics change. It also renders a **coverage matrix** (`build_coverage_matrix()`) — one row per touched zone (loaded DA zones + zones with cached IDA provenance + the primary zone) with a short status per stream (`DA` coverage%, `IDA1/2/3` `source (rows)`, `Reserve (capacity)` product list, `Activation (energy)` product list, `Imbalance (reBAP)` `source (rows)`, or `—`) so a user sees at a glance what is loaded before trusting any number. The reserve column is shown PER zone from the persisted `capacity_price_sources` sidecar (the 6b unified-import path; `read_capacity_sources()`), falling back to the session ancillary's `primary_zone`-only products when a zone has no persisted unified-import capacity; `app.py` passes `anc_df` + `primary_zone` into `data_trust.render`. The `activation_energy` column (3c-1) is likewise per-zone from the `activation_price_sources` sidecar (`read_activation_sources()`) but import-only (no session fallback). The `imbalance_settlement` column (4c) is per-zone from the `imbalance_price_sources` sidecar (`read_imbalance_sources()`), also import-only with no session fallback. When IDA prices are manually uploaded, `build_intraday_source_table()` adds a dedicated sub-table labelling each `(zone, sequence)` as `Manual CSV` so IDA-driven cockpit/uplift numbers stay traceable, and `build_capacity_source_table()` does the same for reserve capacity at `(zone, product, direction)` granularity (reading the same `capacity_price_sources` sidecar), with `build_activation_source_table()` doing likewise for activation energy from the `activation_price_sources` sidecar (plus the 5b `unpriced_nonzero_intervals`/`unpriced_max_volume_mw` columns — the live fetch's dropped-unpriced accounting, window-global per last write, blank for manual imports) and `build_imbalance_source_table()` doing likewise for passive reBAP/imbalance settlement from the `imbalance_price_sources` sidecar. It also renders a **Model assumptions & haircuts** table from `assumptions.build_assumptions_table()` (power/duration/efficiency/capture/capex/dispatch-model from the sidebar; VOM, 365.25-day annualisation, ancillary availability/activation shares, short-gap cap, replay interval cap from config; and the interactive panel knobs — rebid deadband, IDA forecast mode/bucket, rebid share — when set) so every screening number's provenance is answerable in one place. `app.py` builds this table once before the tabs and shares it with both Data Trust and the cockpit export.
- **Simulation Cockpit** (`src/simulation.py`, `src/pages/simulation_cockpit.py`): interval-level replay of the existing MILP dispatch model for a selected local day, plus dispatch event tables and multi-day replay summaries. It reports SoC, physical dispatch, cumulative revenue, throughput/FCE, trade blocks, C-rate, DoD, and screening-level degradation. Treat it as a historical/forward replay cockpit, not live telemetry or real trading execution. Reserve headroom overlay is physical uncommitted headroom only; do not present it as FCR/aFRR activation power without activation data. The multi-day replay and forecast-policy panels each offer an Excel download via `export.cockpit_tables_to_excel()`, which bundles the shown result table plus the model-assumption audit as an `Assumptions` sheet so an exported file is self-documenting. **Step 3B result persistence:** both panels solve only on their Run button and keep the full result bundle (including reserve/triple/stochastic derived rows, export tables and the run-time export assumptions) in `st.session_state`, bound to a content fingerprint (`src/content_fingerprint.py` over the full frames actually read: DA, IDA in DA/IDA mode, and resolved capacity when a reserve product is selected, incl. index dtype/timezone, plus the applicable dates, asset, mode, forecast/deadband/reserve/stochastic knobs and model constants; the chart theme is excluded). Unrelated reruns, theme changes and download clicks re-render without re-solving; any identity change hides the result behind an "Inputs changed since the last run" warning with the export blocked until Run; reverting the inputs restores the stored result; failure/missing-data notices are replayed from the stored result. The forecast-policy panel also renders a **strategy comparison** (`strategy_compare.build_strategy_comparison()`) that reframes the sequential batch's three benchmarks (DA-only / forecast-driven DA+ID / perfect-foresight ceiling) as an investment view — window revenue, annualised EUR/MW/yr (365.25 i.i.d. convention), and uplift% vs DA-only — as a grouped bar chart + table, also included in that panel's export. An **optional fourth row** appears when ancillary capacity prices are loaded: a **DA + reserve-capacity co-optimisation (headroom)** strategy, computed by summing `dispatch.solve_joint_capacity_batch()`'s `joint_total_revenue` over EXACTLY the DA+ID rows' valid days (`per_day["date"]`, via `_reserve_coopt_total()`, so the annualisation denominator matches across all rows). The capacity price itself is also taken from ancillary rows INSIDE that same valid-date window (`_slice_to_local_dates()` before `capacity_price_for_product()`), so an out-of-window capacity print cannot skew the row; when no capacity rows overlap the window the row is omitted (with a UI note) rather than priced off the full sample. This is a **headroom-aware capacity estimate, NOT activation-energy revenue, and NOT additive with DA** — the joint MILP makes reserve headroom compete with DA charge/discharge for the same power budget, so the red-line is enforced by the solver, not by labelling (`ANCILLARY_CAPACITY_AVAILABILITY=0.95` haircut applies; activation energy is not modelled). It is a *different revenue stream* from the DA+IDA rows, **not** a cumulative DA+IDA+reserve ladder; the cockpit caption (`has_reserve` branch) and the export's reserve assumption rows (`_append_reserve_assumptions()`) state this explicitly. The reserve price is `ancillary.capacity_price_for_product()` (duration-weighted mean) for a product chosen from `ancillary.list_capacity_products()`; the row requires an available capacity product in the resolved data and is omitted if none is available; the fetch registry and actual loaded data determine support. A **fifth row** — **DA + IDA1 + reserve (co-opt ceiling)** — is the cumulative perfect-foresight ladder step (Phase 9.2a): `simulation.simulate_da_id_reserve_ceiling_batch()` sums `dispatch.solve_daily_da_id_reserve_dispatch()`, which **introduces no new MILP formulation** (it still calls the existing joint-capacity MILP) — it fuses the DA+ID ceiling accounting (`total = da_gross − implicit_mtm + stage_2_total`, DA financial and marked-to-market at IDA) with Stage 2 = `solve_daily_joint_capacity_lp()` at IDA prices. So reserve headroom competes ONLY with the *physical* Stage-2 schedule (`stage2_charge + stage2_discharge + reserve ≤ power_mw`); the DA financial commitment does NOT occupy physical headroom, and IDA "rebid" is cash settlement of the DA-vs-physical difference, not an extra physical power variable. It collapses to the DA+ID ceiling at `capacity_price=0` and to the DA+reserve row when `IDA==DA`, and satisfies triple ≥ DA+ID ceiling ≥ DA-only (regression-tested). It shares the v1 row's in-window capacity price and valid-day window, needs both in-window IDA and capacity prices, keeps the same red-line (headroom only, no activation energy), and is a **perfect-foresight upper bound, NOT forecast-driven** (`_append_triple_assumptions()` states this in the export). A **sixth row** — **DA + IDA1 + reserve (forecast-driven realistic)** — is the Phase 9.2b reserve-first sequential policy (`simulation.simulate_sequential_da_id_reserve_batch`), ALWAYS walk-forward (reserve is committed D-1 before the DA gate and must not see the target/future day, independent of the panel's LOO/walk-forward toggle, which only scores the DA/IDA skill estimate). The cockpit prices BOTH this row and the 9.2a ceiling (5th row) off the SAME per-interval reserve series (`ancillary.capacity_price_series_for_product()` windowed to the DA+ID valid dates) via `_reserve_triple_totals()`, so the displayed ceiling equals the 9.2b batch's `total_global_ceiling_eur` and **`realistic ≤ ceiling` by construction**; both rows are scored over the 9.2b walk-forward window (their own `triple_valid_days`/`triple_da_baseline` in `build_strategy_comparison()`, which may span one fewer day than the DA/IDA rows). A **forecast-effect gap panel** (`_render_reserve_gap_panel()`) decomposes the full gap (ceiling − realistic) into a SIGNED **Forecast effect** (negative = the forecast helped) plus a commitment-timing cost (`full_gap == forecast_effect + timing_cost`); same red-line, stated in the export via `_append_realistic_triple_assumptions()` (reserve-first, walk-forward, capacity headroom only, no activation energy). A separate **activation-energy overlay** panel (`_render_activation_overlay_section`, 3c-2b; gated on `read_activation_cache`) shows `activation_overlay.compute_activation_overlay()`'s `activation_energy_overlay_eur` + per-(product,direction) breakdown under a capture-share knob, explicitly as a historical replay overlay — NOT co-optimized, NOT additive to the strategy total, no SoC coupling (see the Activation-energy import paragraph). The scenario-based stochastic MILP is now built and surfaced in the cockpit's forecast-policy panel (opt-in **Include stochastic policy** checkbox + rebid-cap slider → a single **Stochastic policy value** row + attribution/risk subpanel; see the stochastic MILP bullet, Increments A–D).
- **Multi-day SoC carry-over** (`simulate_replay_batch`, DA-only and DA+ID): `carry_soc=True` solves each contiguous clean segment as a horizon, reconstructs daily summaries from the solved interval arrays, and seeds the following segment from the previous end-SoC. Terminal-neutral equality is applied at every segment end. Missing/invalid days, changes in dominant daily cadence and the 576-interval cap split segments; `n_cadence_splits` counts only cadence cuts. DA-only replay accepts verified native mixed-duration days and propagates a duration vector through cash, energy, SoC, FEC, VWAP and events. DA/IDA paths retain scalar contracts and two-sided source-row retention checks before solving. `carry_soc=False` retains per-day reset behavior. See `docs/design/replay-input-guards.md` and `docs/design/delivery-duration-v1.md`; the older single-dt description is superseded, and gaps must never become long products.
- **Simplified intraday uplift** (`analytics.calculate_intraday_uplift`): the absolute DA/IDA price-pair estimate remains a separate screening approximation, not a dispatch result. `_intraday_uplift_price_pairs` supplies both statistics and histogram. On each overlapping local day, compatible cadence/phase must be verifiable: different cadences, duplicate timestamps or off-grid observations make the window unavailable; fewer than two observations in either source skip that day. Original rows, including skipped singleton days, remain in both coverage denominators; usable finite pairs determine coverage and the smaller coverage ratio adjusts annual uplift. Zero and one IDA quote on an unverifiable day therefore do not have opposite availability effects. Coverage is an interval-count measure, not delivery-hour coverage. Sequential and stochastic DA/IDA dispatch are already implemented in the cockpit; historical “Phase 2” proposals are not pending work. See `docs/design/replay-input-guards.md`.
- **Sequential DA+ID policy (cockpit Phase 7)** (`dispatch.solve_sequential_da_id_dispatch`, `ida_forecast.build_ida_forecast`, `simulation.simulate_sequential_da_id_batch`): a sequential-heuristic realisation of the Phase-2 idea, surfaced in the Simulation Cockpit's "Forecast-driven IDA policy" expander. The solver runs Stage 1 (commit DA position via the DA-only MILP), Stage 2 (re-dispatch optimally against an IDA *forecast*, not the realised print), then settles the executed schedule at the *realised* IDA price with the same implicit-MtM accounting as the ex-post solver. It returns a three-way decomposition — `da_only_revenue_eur` (baseline), `realised_total_eur` (forecast-driven), `ceiling_total_eur` (= `solve_daily_da_id_dispatch` perfect-foresight upper bound) — with `forecast_error_cost_eur = ceiling - realised >= 0` and `captured_uplift_eur = realised - da_only` (which **can be negative** when the forecast misleads the rebid into churn losses; do NOT clamp it). Identity `captured + forecast_error == ceiling - da_only` always holds. The forecast is a hour-of-day (default, robust on short windows) or hour-of-week (weekday-aware, needs several weeks) climatology mean of the loaded IDA history. `build_ida_forecast`'s `forecast_mode` controls which history each day may see: `"loo"` (default) = leave-one-day-out cross-validation, every day EXCEPT the target — note this is **NOT walk-forward**, it may use future days from the loaded sample, so it is an unbiased *skill* estimate, not what a desk knew in real time; `"walk_forward"` = only days strictly before the target (drops the earliest day); `"in_sample"` = diagnostics only. It reports `coverage` / `n_buckets_filled` (history-backed buckets) / `n_buckets_requested` / `fallback_points` / `forecast_mode` so the UI labels forecast support honestly. **Framing caveat for any UI/doc copy**: arbitrage timing is driven mainly by price *order/shape*, but a forecast *level* error still changes whether a cycle clears (round-trip efficiency loss + VOM make a level shift flip the cycling decision), so do NOT claim "only shape matters". `simulate_sequential_da_id_batch` aggregates this per-day (standalone terminal-neutral, isolating forecast quality from multi-day SoC carry) and reports a guarded `capture_rate` (None when no rebid opportunity exists in the window). It uses **raw solver values** — the cockpit's sidebar DA-slippage capture haircut is intentionally NOT applied to this panel, which the UI states explicitly so the three-way numbers are not confused with the haircut-derated single-day/multi-day replays. **Risk gate / deadband** (`min_rebid_uplift_eur`, exposed in the panel as "Rebid deadband (EUR/MW/day)" × `power_mw`): the desk only rebids when the day's FORECAST-predicted uplift over holding the committed DA schedule exceeds the hurdle; otherwise it holds the DA schedule (`rebid=False`, executed schedule = DA schedule, settling to exactly `da_only`). The forecast uplift is non-negative by construction, and the gate is floored at `_REBID_UPLIFT_EPS_EUR=1e-9` so a zero/solver-noise uplift never registers as a rebid (deterministic `n_rebid_days`/`n_hold_days`); the default `0.0` therefore reproduces the always-rebid baseline (revenue-neutral) while a positive hurdle suppresses churn on marginal days where a thin forecast edge can flip into a realised loss. The batch summary reports `n_rebid_days` / `n_hold_days` and `min_rebid_uplift_eur`. **Forecast skill report** (`ida_forecast.compute_forecast_skill`, exposed via `summary["forecast_skill"]` and a cockpit metrics row + hour-of-day MAE chart): scores the climatology forecast against realised IDA in price space — `mae` / `bias` (signed forecast − realised) / `rmse` (EUR/MWh), `realised_std` for context, a per-local-hour MAE profile, and `skill_vs_da = 1 − MAE_forecast / MAE_da` vs the DA-as-IDA naive baseline (>0 ⇒ the climatology beats the "no rebid signal" null; ≤0 ⇒ the forecast-driven rebid rests on thin ice, so widen the deadband). This makes the deadband threshold an informed choice rather than a guess. The scenario-based stochastic MILP is now built and surfaced in the cockpit's forecast-policy panel (opt-in **Include stochastic policy** checkbox + rebid-cap slider → a single **Stochastic policy value** row + attribution/risk subpanel; see the stochastic MILP bullet, Increments A–D).
- **Reserve price forecast skill** (`reserve_forecast.compute_reserve_forecast_skill`): block-of-day climatology and a flat sample-mean baseline produce MAE/bias/RMSE/coverage and `skill_vs_mean`. The diagnostic's default leave-one-day-out mode is not walk-forward. The implemented reserve-first sequential policy commits reserve under walk-forward forecasts before DA, then prices physical execution under the remaining headroom; it uses per-interval screening decisions, not enforcement of whole 4-hour market bids. Do not infer physical reserve commitment granularity or a market settlement rule from the forecast buckets. The result is compared with the matching perfect-foresight ceiling and a signed forecast-effect gap. Project Case's registered nominal-block settlement is a separate contract.
- **Scenario-based stochastic MILP (implemented v1 and v2)**: the authoritative contracts are `docs/design/stochastic-milp-v1.md` and `docs/design/stochastic-milp-v2-reserve.md`; their dated increment notes preserve review history. The current path includes scenario generation, commitment/execution solvers, batch comparisons and the cockpit/export panel. A rebid cap couples financial DA commitment to physical IDA execution; the same-cap co-opt ceiling is the bound. `policy_value` is a signed delta against the stated capped baseline, not a strategy-revenue total, and must stay out of the revenue-total chart. V2 commits reserve against forecast capacity prices and expected scenario headroom value, then settles capacity at realised prices. It is selected only when the chosen product has non-empty capacity prices inside the valid-date window; otherwise the literal v1 path remains. Reserve mode forces walk-forward information and makes the rebid deadband inert.
- **Stochastic canonical selectors:** v1's Stage-1 selector and v2's Stage-0 selector make equal-objective choices auditable; do not silently remove them or claim a fallback is canonical. Stage 0 keeps all binaries free across its shared-deadline lexicographic passes and accepts only a complete canonical result; fallback retains the first-pass optimum and marks instability. Stage-1 fallbacks affect the diagnostic split; Stage-0 fallbacks can affect the realised policy-value headline. Both counters survive batch/UI/export. Zero forecast capacity can skip Stage 0 before scenario validation; an objective-only perfect-foresight ceiling disables canonical selection and does not use that skip. Preserve the contracts' typed failures, common valid-day comparisons, scenario risk accounting and information-set guards. Historical pre-selector caveats are not the current unconditional behavior. Extending sequential/stochastic scalar `dt` requires a new contract and duration-weighted reserve averages first.

## Commands
- `pip install -r requirements.txt` — install deps (Python 3.11+, Streamlit >=1.55,<2.0; use `.venv` on macOS).
- `python -m pytest tests/ -m "not slow" -q` — fast local regression loop; deselects the solver-heavy tests marked `slow`.
- `python -m pytest tests/ -m slow -q` — run solver-heavy stochastic and Project Case integration/performance checks.
- `python -m pytest tests/ -v` — run the complete suite. Two PDF chart-render checks require `BESS_PULSE_RUN_KALEIDO_TESTS=1` and a working Kaleido/Chrome environment; default skips do not prove rendering success. Revision-specific results are in `docs/validation/current.md`; older handoffs retain their historical counts.
- `python scripts/seed_demo_9_2b.py` — seed a SYNTHETIC DE_LU DA+IDA1+reserve dataset into the cache to validate the Phase 9.2b cockpit panel offline (`--clean` to remove). See `docs/runbooks/validate-9-2b.md`.
- `python -m pytest tests/test_analytics.py::TestDailySpreads -v` — run a single class; swap in `::test_name` for a single test.
- `streamlit run app.py` — launch the dashboard.
- `python -c "from src.data_ingestion import test_elexon_connection; test_elexon_connection()"` — smoke-test Elexon (no API key needed).
- `python -c "from src.data_ingestion import test_entsoe_connection; test_entsoe_connection()"` — smoke-test ENTSO-E (needs `ENTSOE_API_KEY` in `.env`).

CI runs lint, syntax and the complete suite (including `slow`, with coverage) on Python 3.13 for pushes to main and PRs to any base branch. The separate `compat-py311-streamlit1550` job runs a selected panel/chart/caption/cockpit subset from three test files on Python 3.11 / Streamlit 1.55.0. Other dependencies resolve from manifest ranges: this is not a minimum-version matrix or another full suite. Neither job certifies live providers or complete browser layout. Keep both checks green at the reviewed head before merging with authorization. See `.github/workflows/ci.yml`, `CONTRIBUTING.md` and the current validation snapshot.

The September replay corrections are specified in `docs/design/replay-input-guards.md` and `docs/design/delivery-duration-v1.md`; historical single-dt/sparse-grid descriptions must not override them. Step 2 admits verified native mixed DA days, retains Step 1's dominant-daily-cadence cuts and strict DA/IDA guards, discloses DA forecast-comparator coverage, and uses a 720-physical-hour price moving average. Current verification and documentation scope are recorded in `docs/validation/current.md`.

Step 3B is merged in #90 as `cf91374`. Step 3C is merged in #91 as `2e4ed73`: `analytics.calculate_average_price()` is the shared overall DA average for Market Overview, Zone Comparison (including its export), and the Excel/PDF summaries. It validates the entire input grid, requires a real `DatetimeIndex`, and accepts only native 1h/30min/15min durations or the registered SDAC transition. It weights finite prices by their verified durations and discloses finite covered hours out of represented delivery hours via `average_price_basis()`. Bad indices, singleton/unknown/gapped grids and no finite coverage show `n/a` with `avg_price_reason`; never coerce row numbers into timestamps, guess a singleton duration or count non-finite prices as zero. This is separate from the trailing 720-hour chart mean and does not change renewable-conditioned, heatmap or forward-contract averages or solver input contracts.

Step 3A keeps duration-unavailable states visible: `calculate_negative_price_hours` detects unset delivery timestamps before local-day grouping and records `negative_hours_reason`; `negative_price_hours_reason` drives the page and Excel/PDF's shared `n/a` plus explanation. Any unverifiable day/record makes the window's hours unavailable even if that part has no observed negative price. Negative interval count/share and average/most-negative price are not zeroed. `describe_price_index_issue` is shared with the trailing mean: duplicate timestamps suppress the mean with a reason; unsorted/NaT indices also suppress the price chart and its export figure. Classify these defects before calculation rather than catching arbitrary `ValueError` as missing data.

Adjacent Median Price and Std Dev retain their existing equal-row calculations (median and sample standard deviation); their UI/export labels say `row-based`. Do not infer duration weighting from the neighboring Avg Price disclosure.

Step 3C metric layout is scoped to Market Overview, Project Case (including its cockpit mirror), and Simulation Cockpit: these pages use `ui_theme.metric_columns()` to wrap native Streamlit cards at the actual container width; cockpit custom KPI/health grids also use minimum card widths. Project Case keeps full economic section headings and short P10/P50/P90/probability labels, preserving full amounts and units. Browser acceptance at 1280/1440/390 CSS px is recorded separately from AppTest, with sidebar state explicit; string presence alone does not prove readability. See items 32–38 of `docs/runbooks/manual-ui-smoke.md`. The remaining `revenue_estimation`, `forward_scenarios`, `renewable_correlation` and `data_trust` metric rows are not covered by this layout claim; the current validation snapshot records their unverified scope and follow-ups. Step 3D adds the capacity-settlement disclosure below. A manual checklist is a procedure, not blanket acceptance; only the revision-specific evidence records completed checks.

Step 3D is merged in #92 as `fb72dbf` and describes capacity cash with `settlement_disclosure.py`: only the recorded DE_LU Project Case v1 reserve profile asserts six nominal 4h blocks per day; Cockpit capacity-bearing strategies and Revenue joint MILP retain physical-hour screening. Zone/product identity and full text are tied to the result, with short comparison-table columns and export disclosures. DA-only results, standalone annual ancillary estimates, and activation/imbalance energy overlays do not inherit this claim. Unknown reserve profiles are explicitly unverified. Project Case schema/fingerprint/provenance and all cash/energy/SoC algorithms stay unchanged. See the duration contract and manual smoke items 39–43.

## Radar → ESS producer boundary

`project_revenue_handoff_to_json()` exports the immutable result's `screening_cashflow_table.revenue_eur`, never lifecycle `net_eur`. It is signed, whole-modelled-project annual settled revenue after the applicable dispatch/capture/VOM/RTE/availability/floor assumptions, with fingerprints, source-data SHA-256 and canonical JSON digest. ESS owns lifecycle costs, tax, debt and financing. A consumer must not multiply by MW/RTE/availability again, deduct VOM again, or repeat the floor settlement. The producer contract and fixture live in `docs/design/project-revenue-handoff-v1.md` and `tests/fixtures/project_revenue_handoff_v1.json`.

Radar tests its producer and golden fixture. This housekeeping round does not execute or change the ESS consumer; downstream import and valuation remain unverified in this round. Historical cross-repository golden-vector notes do not establish current end-to-end acceptance. Do not change the wire/schema/digest or sibling repository as part of documentation housekeeping.
